<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="StockLhbDetail">

	<!-- Use type aliases to avoid typing the full classname every time. -->
	<typeAlias alias="StockLhbDetail" type="com.wy.stock.domain.StockLhbDetail" />

	<!-- Result maps describe the mapping between the columns returned from 
		a query, and the class properties. A result map isn't necessary if the columns 
		(or aliases) match to the properties exactly. -->
	<resultMap id="StockLhbDetailResult" class="StockLhbDetail">
	   <result property="tradeDate" column="TRADE_DATE" />
		<result property="code" column="CODE" />
		<result property="stockName" column="STOCK_NAME" />
		<result property="close" column="CLOSE" />
		<result property="changePercent" column="CHANGE_PCT" />
		<result property="buyIn" column="BUY_IN" />
		<result property="sellOut" column="SELL_OUT" />
		<result property="volumnAmount" column="VOLUMN_AMOUNT" />
		<result property="volumnAmountIn" column="VOLUMN_AMOUNT_IN" />
		<result property="volumnAmountOut" column="VOLUMN_AMOUNT_OUT" />
		<result property="volumnAmountNet" column="VOLUMN_AMOUNT_NET" />
		<result property="lhType" column="LH_TYPE" />
		<result property="lhTypeDesc" column="LH_TYPE_DESC" />
		<result property="timestamp" column="TIMESTAMP" />
	</resultMap>

	<insert id="insertStockLhbDetail" parameterClass="StockLhbDetail">
		insert into ST_LHB_DETAIL (
		TRADE_DATE,
		CODE,
		STOCK_NAME,
		CLOSE,
		CHANGE_PCT,
		BUY_IN,
		SELL_OUT,
		VOLUMN_AMOUNT,
		VOLUMN_AMOUNT_IN,
		VOLUMN_AMOUNT_OUT,
		VOLUMN_AMOUNT_NET,
		LH_TYPE,
		LH_TYPE_DESC,
		TIMESTAMP)
		values (
		#tradeDate#, #code#, #stockName#, #close#, #changePercent#, #buyIn#, #sellOut#, #volumnAmount#, #volumnAmountIn#, #volumnAmountOut#, #volumnAmountNet#, #lhType#, #lhTypeDesc#, #timestamp#
		)
	</insert>
	
	<delete id="deleteByDate" parameterClass="java.lang.String">
        delete from ST_LHB_DETAIL where TRADE_DATE = #tradeDate#
    </delete>
    
    <select id="query" parameterClass="StockLhbDetail" resultMap="StockLhbDetailResult">
        select *
        from ST_LHB_DETAIL
        WHERE TRADE_DATE  &gt;= #beginDate#
              AND TRADE_DATE &lt;= #endDate#
              ORDER BY TRADE_DATE
    </select>
</sqlMap>