<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="ExchangeInfo">

	<!-- Use type aliases to avoid typing the full classname every time. -->
	<typeAlias alias="ExchangeInfo" type="com.wy.stock.domain.ExchangeInfo" />

	<!-- Result maps describe the mapping between the columns returned from 
		a query, and the class properties. A result map isn't necessary if the columns 
		(or aliases) match to the properties exactly. -->
	<resultMap id="ExchangeInfoResult" class="ExchangeInfo">
		<result property="name" column="NAME" />
		<result property="code" column="CODE" />
		<result property="type" column="TYPE" />
		<result property="exchange" column="EXCHANGE" />
		<result property="timestamp" column="TIMESTAMP" />
	</resultMap>

	<insert id="insertExchangeInfo" parameterClass="ExchangeInfo">
		insert into ST_INFO (
		NAME,
		CODE,
		TYPE,
		EXCHANGE,
		TIMESTAMP)
		values (
		#name#, #code#, #type#, #exchange#,#timestamp#
		)
	</insert>
	
    <delete id="deleteByExchangeType" parameterClass="ExchangeInfo">
        delete from ST_INFO where EXCHANGE = #exchange# AND TYPE = #type#
    </delete>
    
    <delete id="deleteByExchange" parameterClass="java.lang.String">
        delete from ST_INFO where EXCHANGE = #exchange#
    </delete>
    
    <delete id="deleteByKey" parameterClass="ExchangeInfo">
        delete from ST_INFO where EXCHANGE = #exchange# AND CODE = #code# AND TYPE = #type#
    </delete>
    
    <select id="queryAllExchangeInfo" resultMap="ExchangeInfoResult">
        select *
        from ST_INFO
    </select>
    
    <select id="queryExchangeInfoByExchange" resultMap="ExchangeInfoResult">
        select *
        from ST_INFO
       where EXCHANGE = #exchange#
    </select>
    
    <select id="queryExchangeInfoByType" resultMap="ExchangeInfoResult">
        select *
        from ST_INFO
       where TYPE = #type#
    </select>
</sqlMap>